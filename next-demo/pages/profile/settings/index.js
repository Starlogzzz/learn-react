import React, { memo } from 'react'

import ProfileLayout from "../layout"

export default memo(function Settings() {
  return (
    <ProfileLayout>
      <h2>Settings</h2>
    </ProfileLayout>
  )
})
